# Schlep-engine Core API Dependencies (Optimized)
# Minimal set for production API deployment
# Total: ~35 packages (down from 107)

# Core FastAPI stack
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic-settings==2.1.0

# Database & ORM
sqlalchemy==2.0.31
alembic==1.13.2
asyncpg==0.29.0           # PostgreSQL async driver
aiomysql==0.2.0           # MySQL async driver (if needed)

# Authentication & Security
cryptography==41.0.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
pyotp==2.9.0              # 2FA support

# HTTP Client (consolidated)
httpx==0.25.1             # Replaces both aiohttp and requests

# File handling & data processing
aiofiles==23.2.1
pandas==2.2.3
numpy==1.26.4

# Caching & messaging
redis==4.6.0
celery==5.3.4

# Monitoring & logging
prometheus-client==0.19.0
psutil==5.9.6

# Configuration & environment
python-dotenv==1.0.0
pyyaml==6.0.1

# AWS/Cloud integrations (optional - only if using S3)
boto3==1.34.141

# Payment processing (optional - only if using billing)
stripe==7.7.0

# WebSocket support (only if using real-time features)
websockets==12.0

# Email support (optional)
# aiosmtplib==3.0.1

# Image processing (only if handling images)
# pillow==10.1.0

# Additional data formats (only if needed)
# openpyxl==3.1.2          # Excel support
# pyarrow==14.0.1          # Parquet support 