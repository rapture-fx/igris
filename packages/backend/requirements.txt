# Schlep-engine Core API Dependencies (Production Optimized)
# Minimal set for main API server
# Use requirements-ml-optimized.txt for ML service
# Use requirements-dev-optimized.txt for development

# Core FastAPI stack
fastapi
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic-settings==2.1.0

# Database & ORM
sqlalchemy==2.0.31
alembic
asyncpg==0.29.0

# Authentication & Security
cryptography==41.0.0
python-jose[cryptography]
passlib[bcrypt]==1.7.4
pyotp==2.9.0

# HTTP Client (consolidated - replaces aiohttp)
httpx==0.25.1

# Essential data processing
aiofiles==23.2.1
pandas==2.2.3
numpy==1.26.4

# Caching & background tasks
redis==4.6.0
celery==5.3.4

# Monitoring
prometheus-client==0.19.0
psutil==5.9.6

# Configuration
python-dotenv==1.0.0
pyyaml==6.0.1

# Optional services (comment out if not needed)
boto3==1.34.141          # AWS/S3 support
stripe==7.7.0            # Payment processing
websockets==12.0         # Real-time features

# Data format support (add if needed)
openpyxl==3.1.2          # Excel files
pyarrow==14.0.1          # Parquet files

# Added for data analysis
pandas
