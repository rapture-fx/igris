__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Intentionally committed: schema-1 CI uses uv sync --frozen so the Python
# dependency environment must be reproducible from Git alone.
!uv.lock
