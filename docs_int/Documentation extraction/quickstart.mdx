# Quick Start

Get started with Igris Inertial in **5 minutes**. Start with cloud hosted (easiest) or self-host for full control.

---

## Option 1: Cloud Hosted (Fastest)

### Step 1: Start Free Trial

No credit card required. All features unlocked for 14 days.

[Start Free Trial →](https://app.igris-inertial.com/signup)

### Step 2: Get Your API Key

After signup, copy your API key from the dashboard:

```bash
Dashboard → Settings → API Keys → Copy
```

Your API key looks like: `sk-igris-abc123...`

### Step 3: Make Your First Request

**Using cURL:**

```bash
curl -X POST https://api.igris-inertial.com/v1/infer \
  -H "Authorization: Bearer sk-igris-YOUR_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4",
    "messages": [
      {"role": "user", "content": "Explain Thompson Sampling in one sentence"}
    ]
  }'
```

**Using Python (OpenAI SDK):**

```python
from openai import OpenAI

client = OpenAI(
    base_url="https://api.igris-inertial.com/v1",
    api_key="sk-igris-YOUR_KEY"
)

response = client.chat.completions.create(
    model="gpt-4",
    messages=[{"role": "user", "content": "Hello!"}]
)

print(response.choices[0].message.content)
print(f"Cost: ${response.metadata['cost_usd']:.4f}")
print(f"Provider: {response.metadata['provider']}")
```

**Using Node.js:**

```javascript
import OpenAI from 'openai';

const client = new OpenAI({
  baseURL: 'https://api.igris-inertial.com/v1',
  apiKey: 'sk-igris-YOUR_KEY'
});

const response = await client.chat.completions.create({
  model: 'gpt-4',
  messages: [{ role: 'user', content: 'Hello!' }]
});

console.log(response.choices[0].message.content);
console.log(`Cost: $${response.metadata.cost_usd}`);
console.log(`Provider: ${response.metadata.provider}`);
```

**Using Go:**

```go
package main

import (
    "context"
    "fmt"
    "log"

    "github.com/igris-inertial/igris-inertial/internal/sdk/go/igris"
)

func main() {
    client := igris.NewClient(&igris.Config{
        BaseURL: "https://api.igris-inertial.com",
        APIKey:  "sk-igris-YOUR_KEY",
    })

    response, err := client.Infer(context.Background(), &igris.InferRequest{
        Model: "gpt-4",
        Messages: []igris.Message{
            {Role: "user", Content: "Hello!"},
        },
    })

    if err != nil {
        log.Fatal(err)
    }

    fmt.Println(response.Choices[0].Message.Content)
    fmt.Printf("Cost: $%.4f\n", response.Metadata.CostUSD)
    fmt.Printf("Provider: %s\n", response.Metadata.Provider)
}
```

### Step 4: View in Dashboard

Check your dashboard to see:
- Real-time cost tracking
- Routing decisions (which provider was used)
- Latency metrics
- Token usage

[View Dashboard →](https://app.igris-inertial.com/dashboard)

---

## What You Get in the Response

Every request includes metadata about routing:

```json
{
  "id": "chatcmpl-abc123",
  "object": "chat.completion",
  "model": "gpt-4",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "Thompson Sampling is a Bayesian algorithm that balances exploration and exploitation..."
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 12,
    "completion_tokens": 28,
    "total_tokens": 40
  },
  "metadata": {
    "provider": "anthropic",              // Which provider handled the request
    "routing_decision": "thompson-sampling", // How the decision was made
    "cost_usd": 0.00034,                  // Actual cost in USD
    "latency_ms": 187,                    // Request latency
    "trace_id": "550e8400-e29b..."        // Distributed trace ID
  }
}
```

---

## Advanced Usage

### Cost Optimization

Request the cheapest provider that meets quality standards:

```bash
curl -X POST https://api.igris-inertial.com/v1/infer \
  -H "Authorization: Bearer sk-igris-YOUR_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-3.5-turbo",
    "messages": [{"role": "user", "content": "Simple question"}],
    "optimization": "cost"
  }'
```

### Latency Optimization

Request the fastest provider:

```bash
curl -X POST https://api.igris-inertial.com/v1/infer \
  -H "Authorization: Bearer sk-igris-YOUR_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4",
    "messages": [{"role": "user", "content": "Urgent question"}],
    "optimization": "latency"
  }'
```

### Speculative Execution (Growth+ Tier)

Race multiple providers for 60% faster time-to-first-token:

```bash
curl -X POST https://api.igris-inertial.com/v1/infer \
  -H "Authorization: Bearer sk-igris-YOUR_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4",
    "messages": [{"role": "user", "content": "Fast response needed"}],
    "speculative_mode": "latency"
  }'
```

### Streaming Responses

Get tokens as they're generated:

```python
from openai import OpenAI

client = OpenAI(
    base_url="https://api.igris-inertial.com/v1",
    api_key="sk-igris-YOUR_KEY"
)

response = client.chat.completions.create(
    model="gpt-4",
    messages=[{"role": "user", "content": "Tell me a story"}],
    stream=True
)

for chunk in response:
    if chunk.choices[0].delta.content:
        print(chunk.choices[0].delta.content, end='', flush=True)
```

---

## Option 2: Self-Hosted

Deploy Igris Inertial in your own infrastructure (Scale tier required).

### Prerequisites

- Docker & Docker Compose
- Or: Kubernetes cluster with Helm

### Docker Compose (Quickest)

```bash
# Clone repository
git clone https://github.com/igris-inertial/igris-inertial
cd igris-inertial

# Configure environment
cp .env.example .env
# Edit .env with your provider API keys

# Start services
docker-compose up -d

# Verify health
curl http://localhost:8081/v1/health
```

**Services started:**
- API Gateway: `localhost:8081`
- Prometheus: `localhost:9090`
- Grafana: `localhost:3000`
- PostgreSQL: `localhost:5432`
- Redis: `localhost:6379`

### Kubernetes (Production)

```bash
# Add Helm repo
helm repo add igris-inertial https://charts.igris-inertial.com
helm repo update

# Install with default values
helm install igris-inertial igris-inertial/igris-inertial \
  --set postgresql.enabled=true \
  --set redis.enabled=true \
  --set ingress.enabled=true \
  --set ingress.host=api.yourcompany.com

# Check status
kubectl get pods -l app=igris-inertial
```

### Configuration

Set your provider API keys:

```bash
# Using environment variables
export OPENAI_API_KEY="sk-..."
export ANTHROPIC_API_KEY="sk-ant-..."

# Or using Kubernetes secrets
kubectl create secret generic igris-provider-keys \
  --from-literal=openai-key=sk-... \
  --from-literal=anthropic-key=sk-ant-...
```

### Verify Installation

```bash
# Health check
curl http://localhost:8081/v1/health

# List available models
curl http://localhost:8081/v1/models

# Test inference
curl -X POST http://localhost:8081/v1/infer \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4",
    "messages": [{"role": "user", "content": "Hello"}]
  }'
```

---

## Enable Advanced Features

### Thompson Sampling Optimizer

Switch from Go router to Rust-powered Thompson Sampling:

```bash
curl -X POST http://localhost:8081/admin/optimizer \
  -H "X-Admin-Token: YOUR_ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "mode": "rust",
    "sample_rate": 1.0
  }'
```

**Modes:**
- `go` - Basic Go router (default)
- `shadow` - Run Rust in parallel for testing (no impact on routing)
- `rust` - Use Rust optimizer for all requests

### Check Optimizer Status

```bash
curl http://localhost:8081/admin/optimizer/status \
  -H "X-Admin-Token: YOUR_ADMIN_TOKEN"
```

**Response:**
```json
{
  "mode": "rust",
  "sample_rate": 1.0,
  "total_requests": 10523,
  "rust_requests": 10523,
  "go_fallbacks": 12,
  "avg_latency_ms": 234,
  "cost_savings_percent": 32.4
}
```

---

## Monitor Your System

### Prometheus Metrics

View metrics at `http://localhost:9090`

**Key metrics:**
- `http_requests_total{method, path, status}`
- `inference_requests_total{provider, model, status}`
- `inference_latency_ms{provider, model}`
- `cost_usd_total{provider, tenant}`

### Grafana Dashboards

Access Grafana at `http://localhost:3000`

**Default credentials:**
- Username: `admin`
- Password: `admin`

**Pre-built dashboards:**
1. Request Overview - RPS, latency, errors
2. Provider Performance - Per-provider metrics
3. Cost Analytics - Spend tracking
4. Thompson Sampling - Routing decisions

### View Logs

```bash
# Docker Compose
docker-compose logs -f igris-api

# Kubernetes
kubectl logs -f deployment/igris-inertial -c api
```

---

## Testing with Different Providers

### Benchmark Mode (No API Keys Needed)

Test without real API calls:

```bash
export PROVIDER_MODE=benchmark
./igris-api
```

Benchmark mode simulates:
- Realistic latency profiles
- Token counting
- Cost calculation
- Provider errors/failures

### Real Mode (With API Keys)

Use actual provider APIs:

```bash
export PROVIDER_MODE=real
export OPENAI_API_KEY="sk-..."
export ANTHROPIC_API_KEY="sk-ant-..."
./igris-api
```

---

## Next Steps

### 1. Configure Providers

Add your provider API keys with BYOK:
[Providers & Keys →](/docs/providers-keys)

### 2. Set Up Routing Policies

Configure Thompson Sampling, semantic routing, or custom policies:
[Routing Policies →](/docs/routing-policies)

### 3. Enable Multi-Tenancy

Set up tenant isolation and per-tenant budgets:
[Multi-Tenancy →](/docs/multi-tenancy)

### 4. Monitor & Optimize

Set up dashboards and alerts:
[Observability →](/docs/observability)

---

## Common Issues

### Port Already in Use

```bash
# Check what's using port 8081
lsof -i :8081

# Kill the process
kill -9 <PID>
```

### Connection Refused

Ensure all services are running:

```bash
# Docker Compose
docker-compose ps

# Kubernetes
kubectl get pods
```

### Authentication Failed

Check your API key:

```bash
# Test with curl
curl -H "Authorization: Bearer sk-igris-YOUR_KEY" \
  https://api.igris-inertial.com/v1/health
```

### High Latency

Enable Rust optimizer and speculative execution:

```bash
# Enable Rust optimizer
curl -X POST http://localhost:8081/admin/optimizer \
  -H "X-Admin-Token: YOUR_ADMIN_TOKEN" \
  -d '{"mode": "rust", "sample_rate": 1.0}'

# Use speculative mode in requests
curl -X POST http://localhost:8081/v1/infer \
  -d '{"model": "gpt-4", "messages": [...], "speculative_mode": "latency"}'
```

---

## Get Help

- **Documentation:** [docs.igris-inertial.com](https://docs.igris-inertial.com)
- **Discord:** [Join community](https://discord.gg/igris-inertial)
- **GitHub Issues:** [Report a bug](https://github.com/igris-inertial/igris-inertial/issues)
- **Email:** support@igris-inertial.com
