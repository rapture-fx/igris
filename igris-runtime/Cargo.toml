[workspace]
members = [
    "crates/igris-core",
    "crates/igris-routing",
    "crates/igris-server",
    "crates/igris-emergency",
    "crates/igris-tools",
    "crates/igris-license-client",
    "crates/mcp-server",
    "crates/mcp-client",
    "crates/igris-rt",
    "crates/igris-recovery",
    "crates/igris-wal",
]
resolver = "2"

[workspace.package]
version = "1.6.0"
edition = "2021"
rust-version = "1.75"
authors = ["Igris Runtime Team"]
license = "MIT OR Apache-2.0"

[workspace.dependencies]
# Core async runtime
tokio = { version = "1.35", features = ["rt-multi-thread", "macros", "time", "sync", "process", "io-util"] }
tokio-util = "0.7"

# Web framework
axum = { version = "0.7", features = ["macros", "ws"] }
tower = { version = "0.4", features = ["timeout", "limit", "buffer", "util"] }
tower-http = { version = "0.5", features = ["trace", "cors", "compression-gzip"] }

# OpenAPI documentation
utoipa = { version = "4.2", features = ["axum_extras"] }
utoipa-swagger-ui = { version = "6.0", features = ["axum"] }

# TLS — ring backend is pure Rust, cross-compiles cleanly to musl/ARM
rustls = { version = "0.22", features = ["ring"] }

# Embedded database
redb = "2.0"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
json5 = "0.4"

# Cryptography
ed25519-dalek = { version = "2.1", features = ["rand_core"] }
aes-gcm = "0.10"
sha2 = "0.10"
rand = "0.8"
base64 = "0.22"

# Utilities
anyhow = "1.0"
thiserror = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# HTTP client for providers
reqwest = { version = "0.11", default-features = false, features = ["rustls-tls", "json", "stream"] }

# Async utilities
futures = "0.3"

# Regex for env var expansion
regex = "1.10"

# UUID generation
uuid = { version = "1.6", features = ["v4", "serde"] }

# Async streaming
async-stream = "0.3"

# mDNS service discovery
mdns-sd = "0.11"

[profile.release]
opt-level = "z"        # Optimize for size
lto = "fat"            # Full link-time optimization
codegen-units = 1      # Single codegen unit for max optimization
panic = "abort"        # No unwinding
strip = true           # Strip symbols

[profile.release.package."*"]
opt-level = "z"        # Aggressive size optimization for all dependencies
