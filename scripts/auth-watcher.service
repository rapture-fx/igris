[Unit]
Description=Igris Auth Log Watcher
After=docker.service
Requires=docker.service

[Service]
User=wira
EnvironmentFile=/home/wira/igris/.env.production
ExecStart=/home/wira/igris/scripts/auth-watcher.sh
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
