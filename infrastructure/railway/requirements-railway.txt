# Railway-optimized requirements for ML workloads
# Based on production validation requirements

# Core FastAPI (production-ready versions)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
gunicorn==21.2.0

# Database (optimized for Railway + Supabase)
sqlalchemy==2.0.23
alembic==1.13.1
psycopg2-binary==2.9.9
redis==5.0.1
asyncpg==0.29.0  # Async PostgreSQL for better performance

# Authentication and security (production-hardened)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
cryptography==45.0.5

# Background processing (Railway-optimized)
celery==5.3.4
flower==2.0.1
redis-py-cluster==2.1.3

# Cloud storage (Railway-compatible)
google-cloud-storage==2.10.0
boto3==1.34.0

# ML Core (optimized for Railway's compute environment)
numpy==2.3.2
pandas==2.0.3
scikit-learn==1.7.1

# Deep Learning (Railway GPU-ready, fallback to CPU)
tensorflow-cpu==2.15.0  # CPU version for Railway
torch==2.1.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu
transformers==4.36.0

# Document processing (validated performance)
pdfplumber==0.9.0
PyPDF2==3.0.1
python-docx==0.8.11
openpyxl==3.1.2
pytesseract==0.3.10
Pillow==10.1.0

# Production monitoring
prometheus-client==0.19.0
sentry-sdk[fastapi]==1.38.0
psutil==5.9.6
httpx==0.25.2

# Reliability (enterprise-grade)
tenacity==8.2.3
circuit-breaker==1.4.0
structlog==23.2.0
python-json-logger==2.0.7

# Railway-specific optimizations
orjson==3.9.10  # Faster JSON processing
lz4==4.3.2      # Faster compression for caching
msgpack==1.0.7  # Efficient serialization

# Production utilities
python-dotenv==1.0.0
requests==2.31.0
aiofiles==23.2.1
email-validator==2.1.0