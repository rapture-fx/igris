apiVersion: apps/v1
kind: Deployment
metadata:
  name: schlep-engine-frontend
  labels:
    app: schlep-engine-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: schlep-engine-frontend
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
      labels:
        app: schlep-engine-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/your-org/schlep-engine-frontend:latest # Replace in CI/CD when pushing
          ports:
            - containerPort: 3000
          env:
            - name: NEXT_PUBLIC_API_URL
              value: "https://api.schlep-engine.com" # adjust per environment
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
---
apiVersion: v1
kind: Service
metadata:
  name: schlep-engine-frontend
spec:
  selector:
    app: schlep-engine-frontend
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP 