---
title: "GET /v1/routing/circuit-breaker/status"
description: "Returns aggregate and per-provider circuit-breaker state. If no provider rows exist yet, the endpoint returns a default closed state so dashboards can render on a fresh deployment."
audience: "public"
---

# GET /v1/routing/circuit-breaker/status

Returns aggregate and per-provider circuit-breaker state. If no provider rows exist yet, the endpoint returns a default closed state so dashboards can render on a fresh deployment.

<div className="api-meta not-prose"><div className="api-meta-group"><span className="api-meta-label">Method</span><span className="api-badge api-method-get">GET</span></div><div className="api-meta-group"><span className="api-meta-label">Path</span><span className="api-meta-value">/v1/routing/circuit-breaker/status</span></div><div className="api-meta-group"><span className="api-meta-label">Base URL</span><span className="api-meta-value">https://overture.igrisinertial.com</span></div><div className="api-meta-group"><span className="api-meta-label">Auth</span><span className="api-meta-value">Session cookie or igris_ API key</span></div><div className="api-meta-group"><span className="api-meta-label">Surface</span><span className="api-badge api-surface-cloud-api">Cloud API</span></div><div className="api-meta-group"><span className="api-meta-label">Stability</span><span className="api-badge api-stability-stable">stable</span></div><div className="api-meta-group"><span className="api-meta-label">Support</span><span className="api-badge api-support-supported">supported</span></div><div className="api-meta-group"><span className="api-meta-label">Deployment</span><span className="api-badge api-deployment-cloud">cloud</span></div></div>

## When To Use

Use this endpoint when the operation belongs to the Routing Control & Analytics surface on the hosted API. It is intended to be the customer-facing contract for this capability.

## Integration Guidance

Safe retries are usually reasonable for transient 5xx or 429 responses. Do not retry 4xx responses until you have fixed the request, credentials, or addressed resource.

### Common Mistakes

- Using the wrong credential type or forgetting to send the expected API key header.

### Related Endpoints

- [GET /v1/routing/stats](/docs/api-reference/routing-control-analytics/get-v1-routing-stats)
- [GET /v1/routing/recent](/docs/api-reference/routing-control-analytics/get-v1-routing-recent)
- [GET /v1/routing/leaderboard](/docs/api-reference/routing-control-analytics/get-v1-routing-leaderboard)

### Related Guides

- [API Authentication](/docs/api-reference/authentication)
- [API Errors](/docs/api-reference/errors)
- [Routing Engine](/docs/escapevector)

## Code Examples

### cURL

```bash
curl -X GET https://overture.igrisinertial.com/v1/routing/circuit-breaker/status \
  -H "Authorization: Bearer $IGRIS_API_KEY"
```

### JavaScript

```javascript
const response = await fetch('https://overture.igrisinertial.com/v1/routing/circuit-breaker/status', {
  method: 'GET',
  headers: {
    'Authorization': `Bearer ${process.env.IGRIS_API_KEY}`,
  },
});

const data = await response.text();
console.log(data);
```

### Go

```go
client := &http.Client{}
body := http.NoBody

req, err := http.NewRequest("GET", "https://overture.igrisinertial.com/v1/routing/circuit-breaker/status", body)
if err != nil {
	log.Fatal(err)
}

req.Header.Set("Authorization", "Bearer " + os.Getenv("IGRIS_API_KEY"))

resp, err := client.Do(req)
if err != nil {
	log.Fatal(err)
}
defer resp.Body.Close()

payload, _ := io.ReadAll(resp.Body)
fmt.Println(string(payload))
```

### Rust

```rust
let client = reqwest::Client::new();
let response = client
        .request(reqwest::Method::GET, "https://overture.igrisinertial.com/v1/routing/circuit-breaker/status")
        .header("Authorization", format!("Bearer {}", std::env::var("IGRIS_API_KEY")?))
        .send()
        .await?;

let payload = response.text().await?;
println!("{}", payload);
```

## Sample Response

```json
{
  "enabled": true,
  "state": "closed",
  "trip_count": 0,
  "providers": [
    {
      "provider": "openai",
      "state": "closed",
      "failure_count": 0,
      "trip_count": 0
    }
  ]
}
```

## Status Codes

| Code | Title | Description |
|---|---|---|
| 200 | Success | The request completed successfully. For streaming or download endpoints, the success response may be a stream or attachment rather than a JSON object. |
| 400 | Bad Request | The route was reached, but the request body, query string, or path parameter did not match the endpoint contract. |
| 401 | Unauthorized | Credentials were missing, expired, malformed, or not accepted by this route. |
| 429 | Too Many Requests | The caller exceeded the current throttle window and should wait before retrying. |
| 500 | Server Error | The server accepted the request contract but failed while processing it. |

