---
title: "DELETE /api/v1/runtime/deregister"
description: "Marks a runtime as deregistered and unhealthy for the current tenant. The runtime remains in historical records but should no longer be treated as an active fleet member."
audience: "public"
---

# DELETE /api/v1/runtime/deregister

Marks a runtime as deregistered and unhealthy for the current tenant. The runtime remains in historical records but should no longer be treated as an active fleet member.

<div className="api-meta not-prose"><div className="api-meta-group"><span className="api-meta-label">Method</span><span className="api-badge api-method-delete">DELETE</span></div><div className="api-meta-group"><span className="api-meta-label">Path</span><span className="api-meta-value">/api/v1/runtime/deregister</span></div><div className="api-meta-group"><span className="api-meta-label">Base URL</span><span className="api-meta-value">https://overture.igrisinertial.com</span></div><div className="api-meta-group"><span className="api-meta-label">Auth</span><span className="api-meta-value">igris_ API key via X-API-Key or Authorization: Bearer</span></div><div className="api-meta-group"><span className="api-meta-label">Surface</span><span className="api-badge api-surface-fleet-management">Fleet management</span></div><div className="api-meta-group"><span className="api-meta-label">Stability</span><span className="api-badge api-stability-stable">stable</span></div><div className="api-meta-group"><span className="api-meta-label">Support</span><span className="api-badge api-support-supported">supported</span></div><div className="api-meta-group"><span className="api-meta-label">Deployment</span><span className="api-badge api-deployment-hybrid">hybrid</span></div></div>

## When To Use

Use this endpoint when the workflow spans both the hosted control plane and one or more runtimes. It belongs to the Runtime Distribution & Fleet Coordination area of the product and is most useful when you are coordinating runtime distribution, fleet state, or device operations.

## Integration Guidance

Do not blindly retry write operations. Retry only on transient 5xx or 429 responses, and make sure the client is idempotent or otherwise safe to replay.

### Common Mistakes

- Using the wrong credential type or forgetting to send the expected API key header.
- Treating a hybrid coordination route like a local-only runtime call without the required tenant context or fleet state.
- Replaying a write request without thinking through idempotency, duplication, or partial success.

### Related Endpoints

- [GET /v1/runtime/install](/docs/api-reference/runtime-distribution-fleet-coordination/get-v1-runtime-install)
- [GET /v1/runtime/checksum](/docs/api-reference/runtime-distribution-fleet-coordination/get-v1-runtime-checksum)
- [GET /v1/runtime/download](/docs/api-reference/runtime-distribution-fleet-coordination/get-v1-runtime-download)

### Related Guides

- [API Authentication](/docs/api-reference/authentication)
- [API Errors](/docs/api-reference/errors)
- [Hybrid Deployment Workflow](/docs/hybrid-deployment-workflow)

## Request Body

| Field | Type | Required | Description |
|---|---|---|---|
| `machine_id` | `string` | Yes | Stable runtime machine identifier used during registration. |

## Request Example

```json
{
  "machine_id": "dev_0f4c3f1a2d9b45cf"
}
```

## Code Examples

### cURL

```bash
curl -X DELETE https://overture.igrisinertial.com/api/v1/runtime/deregister \
  -H "X-API-Key: $IGRIS_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "machine_id": "dev_0f4c3f1a2d9b45cf"
}'
```

### JavaScript

```javascript
const response = await fetch('https://overture.igrisinertial.com/api/v1/runtime/deregister', {
  method: 'DELETE',
  headers: {
    'X-API-Key': process.env.IGRIS_API_KEY,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
  "machine_id": "dev_0f4c3f1a2d9b45cf"
}),
});

const data = await response.text();
console.log(data);
```

### Go

```go
client := &http.Client{}
body := strings.NewReader("{\n  \"machine_id\": \"dev_0f4c3f1a2d9b45cf\"\n}")

req, err := http.NewRequest("DELETE", "https://overture.igrisinertial.com/api/v1/runtime/deregister", body)
if err != nil {
	log.Fatal(err)
}
req.Header.Set("Content-Type", "application/json")
req.Header.Set("X-API-Key", os.Getenv("IGRIS_API_KEY"))

resp, err := client.Do(req)
if err != nil {
	log.Fatal(err)
}
defer resp.Body.Close()

payload, _ := io.ReadAll(resp.Body)
fmt.Println(string(payload))
```

### Rust

```rust
let client = reqwest::Client::new();
let response = client
        .request(reqwest::Method::DELETE, "https://overture.igrisinertial.com/api/v1/runtime/deregister")
        .header("X-API-Key", std::env::var("IGRIS_API_KEY")?)
        .json(&serde_json::json!({
  "machine_id": "dev_0f4c3f1a2d9b45cf"
}))
        .send()
        .await?;

let payload = response.text().await?;
println!("{}", payload);
```

## Sample Response

```json
{
  "status": "ok"
}
```

## Status Codes

| Code | Title | Description |
|---|---|---|
| 200 | Success | The runtime was marked deregistered, or it was already no longer active. |
| 400 | Bad request | The request body was invalid or missing `machine_id`. |
| 401 | Unauthorized | The tenant API key is missing or invalid. |

