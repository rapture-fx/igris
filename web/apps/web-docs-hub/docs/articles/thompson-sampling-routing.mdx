---
title: How Adaptive Routing Works
date: 2026-03-20
summary: How Igris automatically routes requests to the best provider for your workload based on real-time performance data.
author: Igris Team
---

# How Adaptive Routing Works

When you configure multiple AI providers with Igris, you face a fundamental question: which provider should handle each request? The answer depends on constantly changing factors — provider latency, error rates, cost, and your specific workload patterns.

Igris solves this automatically using an adaptive routing algorithm that learns from every request.

---

## The Problem

Imagine you have three providers configured:

| Provider | Avg Latency | Cost per 1K tokens |
|---|---|---|
| Anthropic Claude | 280ms | $0.015 |
| OpenAI GPT-4 | 350ms | $0.030 |
| Google Gemini | 200ms | $0.010 |

A naive approach might always pick the fastest (Gemini) or the cheapest (Gemini). But what if:
- Gemini occasionally times out under load?
- Claude produces higher-quality responses for your specific prompts?
- OpenAI is faster for certain model types?

Static rules can't adapt to these dynamics. Igris's adaptive routing can.

---

## How It Works

Igris maintains real-time performance statistics for each configured provider: latency, success rate, and cost. On each request, the routing engine selects the best provider based on recent observations — automatically balancing between providers it knows are reliable and occasionally trying others to keep its data fresh.

After each request completes, the provider's statistics are updated with the observed result. A provider that performs well receives more traffic. A provider that degrades is automatically routed around.

---

## Why This Matters for Your Workload

### Automatic Adaptation

If a provider's latency degrades, the router automatically reduces its traffic share. No manual intervention needed. When the provider recovers, traffic gradually returns.

### Handles New Providers Gracefully

When you add a new provider, the router explores it to gather performance data, then settles into a steady pattern once it understands the provider's characteristics.

### Balances Cost and Performance

The algorithm naturally finds the sweet spot between cost and performance for your specific workload. If a cheaper provider is "good enough," it will receive more traffic. If quality matters most, higher-performing providers dominate.

---

## Configuration

Adaptive routing is the default. You can also select a specific strategy:

```json
{
  "routing": {
    "strategy": "thompson-sampling",
    "fallback_chain": ["anthropic", "openai", "local"]
  }
}
```

| Field | Description |
|---|---|
| `strategy` | `"thompson-sampling"` (adaptive, default), `"cost-optimized"`, `"latency-optimized"` |
| `fallback_chain` | Ordered list of providers to try if the selected one fails |

---

## Monitoring

View routing decisions in real time:

- **Console**: `/models/routing` shows provider weights and selection history
- **API**: `GET /v1/routing/stats` returns per-provider request counts and latency
- **API**: `GET /v1/routing/leaderboard` shows the current provider ranking

---

[Cloud Coordination →](/docs/cloud-coordination) · [All Articles →](/docs/articles)
