---
title: "POST /v1/runtime/task/stream"
description: "Submits a streaming durable task directly to the local runtime executor. This endpoint belongs to the Local Runtime API group and is documented here so customers can understand the contract, auth model, and verified sample traffic without reading the server implementation."
---

# POST /v1/runtime/task/stream

Submits a streaming durable task directly to the local runtime executor. This endpoint belongs to the Local Runtime API group and is documented here so customers can understand the contract, auth model, and verified sample traffic without reading the server implementation.

<div className="api-meta not-prose"><div className="api-meta-group"><span className="api-meta-label">Method</span><span className="api-badge api-method-post">POST</span></div><div className="api-meta-group"><span className="api-meta-label">Path</span><span className="api-meta-value">/v1/runtime/task/stream</span></div><div className="api-meta-group"><span className="api-meta-label">Base URL</span><span className="api-meta-value">http://localhost:8080</span></div><div className="api-meta-group"><span className="api-meta-label">Auth</span><span className="api-meta-value">Runtime-config dependent</span></div><div className="api-meta-group"><span className="api-meta-label">Surface</span><span className="api-badge api-surface-local-runtime">Local runtime</span></div><div className="api-meta-group"><span className="api-meta-label">Stability</span><span className="api-badge api-stability-stable">stable</span></div><div className="api-meta-group"><span className="api-meta-label">Support</span><span className="api-badge api-support-supported">supported</span></div><div className="api-meta-group"><span className="api-meta-label">Deployment</span><span className="api-badge api-deployment-local">local</span></div></div>

## When To Use

Use this endpoint when the operation belongs on the local runtime rather than the hosted API. It is part of the Local Runtime API surface and should be called from the environment where the runtime is actually serving traffic.

## Integration Guidance

Do not blindly retry write operations. Retry only on transient 5xx or 429 responses, and make sure the client is idempotent or otherwise safe to replay.

### Common Mistakes

- Using the wrong credential type or forgetting to send the expected API key header.
- Pointing the client at the hosted base URL even though this route is served by the local runtime.
- Replaying a write request without thinking through idempotency, duplication, or partial success.

### Related Endpoints

- [GET /v1/health](/docs/api-reference/local-runtime-api/get-v1-health)
- [GET /v1/runtime/profile](/docs/api-reference/local-runtime-api/get-v1-runtime-profile)
- [POST /v1/chat/completions](/docs/api-reference/local-runtime-api/post-v1-chat-completions)

### Related Guides

- [API Authentication](/docs/api-reference/authentication)
- [API Errors](/docs/api-reference/errors)
- [Deploy Local Runtime](/docs/deploy-local-runtime)

## Request Body

| Field | Type | Required | Description |
|---|---|---|---|
| `task_id` | `string` | Yes | Unique durable task identifier supplied by the caller. |
| `task_type` | `object` | Yes | Tagged runtime task payload. Streaming currently supports `{"type":"single_inference"}` with `stream:true`. |
| `idempotency_key` | `string` | Yes | Stable key used to replay a completed stream result or reject conflicting submissions. |
| `tenant_id` | `string` | Yes | Tenant or local isolation identifier used for idempotency storage. |
| `deadline_ms` | `integer` | No | Optional execution deadline in milliseconds. |
| `containment` | `object` | No | Optional runtime bounds forwarded from the control plane. |

## Request Example

```json
{
  "task_id": "018f4a2b-3c1e-7a2d-9b8f-4d5e6f7a8b9c",
  "task_type": {
    "type": "single_inference",
    "stream": true,
    "model": "local",
    "messages": [
      {
        "role": "user",
        "content": "hello"
      }
    ]
  },
  "idempotency_key": "stream-018f4a2b",
  "tenant_id": "tenant-local"
}
```

## Notes

- Successful responses are Server-Sent Events and include `X-Igris-Runtime-Task-Id`, `X-Igris-Runtime-Stream-Resume-Supported`, and `X-Igris-Runtime-Stream-Replay-Condition` headers.
- The stream emits ordinary chat chunks first, then an `event: task_result` payload with the durable task result and `durability` metadata, followed by `data: [DONE]`.
- If the same idempotency key is replayed after a completed stream with final output, Runtime replays the final output and task result. If the stored task is failed, checkpointed, or lacks final output, Runtime returns `409 stream_replay_unavailable` with a task snapshot and durability metadata.

## Code Examples

### cURL

```bash
curl -X POST http://localhost:8080/v1/runtime/task/stream \
  -H "Authorization: Bearer $IGRIS_RUNTIME_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "task_id": "018f4a2b-3c1e-7a2d-9b8f-4d5e6f7a8b9c",
  "task_type": {
    "type": "single_inference",
    "stream": true,
    "model": "local",
    "messages": [
      {
        "role": "user",
        "content": "hello"
      }
    ]
  },
  "idempotency_key": "stream-018f4a2b",
  "tenant_id": "tenant-local"
}'
```

### JavaScript

```javascript
const response = await fetch('http://localhost:8080/v1/runtime/task/stream', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${process.env.IGRIS_RUNTIME_API_KEY}`,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
  "task_id": "018f4a2b-3c1e-7a2d-9b8f-4d5e6f7a8b9c",
  "task_type": {
    "type": "single_inference",
    "stream": true,
    "model": "local",
    "messages": [
      {
        "role": "user",
        "content": "hello"
      }
    ]
  },
  "idempotency_key": "stream-018f4a2b",
  "tenant_id": "tenant-local"
}),
});

const data = await response.text();
console.log(data);
```

### Go

```go
client := &http.Client{}
body := strings.NewReader("{\n  \"task_id\": \"018f4a2b-3c1e-7a2d-9b8f-4d5e6f7a8b9c\",\n  \"task_type\": {\n    \"type\": \"single_inference\",\n    \"stream\": true,\n    \"model\": \"local\",\n    \"messages\": [\n      {\n        \"role\": \"user\",\n        \"content\": \"hello\"\n      }\n    ]\n  },\n  \"idempotency_key\": \"stream-018f4a2b\",\n  \"tenant_id\": \"tenant-local\"\n}")

req, err := http.NewRequest("POST", "http://localhost:8080/v1/runtime/task/stream", body)
if err != nil {
	log.Fatal(err)
}
req.Header.Set("Content-Type", "application/json")
req.Header.Set("Authorization", "Bearer " + os.Getenv("IGRIS_RUNTIME_API_KEY"))

resp, err := client.Do(req)
if err != nil {
	log.Fatal(err)
}
defer resp.Body.Close()

payload, _ := io.ReadAll(resp.Body)
fmt.Println(string(payload))
```

### Rust

```rust
let client = reqwest::Client::new();
let response = client
        .request(reqwest::Method::POST, "http://localhost:8080/v1/runtime/task/stream")
        .header("Authorization", format!("Bearer {}", std::env::var("IGRIS_RUNTIME_API_KEY")?))
        .json(&serde_json::json!({
  "task_id": "018f4a2b-3c1e-7a2d-9b8f-4d5e6f7a8b9c",
  "task_type": {
    "type": "single_inference",
    "stream": true,
    "model": "local",
    "messages": [
      {
        "role": "user",
        "content": "hello"
      }
    ]
  },
  "idempotency_key": "stream-018f4a2b",
  "tenant_id": "tenant-local"
}))
        .send()
        .await?;

let payload = response.text().await?;
println!("{}", payload);
```

## Sample Response

```text
data: {"choices":[{"delta":{"content":"Hello"}}]}

event: task_result
data: {"task_id":"018f4a2b-3c1e-7a2d-9b8f-4d5e6f7a8b9c","status":"completed","durability":{"mode":"streaming","resume_supported":false,"replay_supported":true,"replay_condition":"completed-final-output","checkpoint_persisted":false}}

data: [DONE]

```

## Status Codes

| Code | Title | Description |
|---|---|---|
| 200 | SSE stream | Runtime accepted the stream and returns Server-Sent Events with a terminal `task_result` event. |
| 400 | Invalid streaming request | The request was not a supported streaming `single_inference` task or attempted unsupported stream resume. |
| 401 | Unauthorized | Runtime-local authentication rejected the request. |
| 409 | Replay unavailable | The idempotency key already has a stored task, but it cannot be replayed as a completed final-output stream. |
| 500 | Runtime execution failed | Runtime accepted the request but failed while processing the stream. |

### 409 Example

```json
{
  "error": {
    "message": "Streaming replay is only available for completed task submissions with final output",
    "type": "stream_replay_unavailable"
  },
  "task": {
    "task_id": "018f4a2b-3c1e-7a2d-9b8f-4d5e6f7a8b9c",
    "status": {
      "status": "failed",
      "reason": "provider stream failed"
    },
    "final_output_available": false,
    "failure_details": {
      "source": "runtime",
      "operation": "execution",
      "rejection_type": "step_failed",
      "message": "provider stream failed",
      "step_index": 0,
      "domain": "agent",
      "node_id": "agent-0"
    }
  },
  "durability": {
    "mode": "streaming",
    "resume_supported": false,
    "replay_supported": false,
    "replay_condition": "completed-final-output",
    "checkpoint_persisted": false
  }
}
```

