---
title: "GET /v1/vault/keys"
description: "Lists the provider keys currently stored for the tenant. Returned entries are masked and operationally useful, but they are not a secret-retrieval mechanism."
---

# GET /v1/vault/keys

Lists the provider keys currently stored for the tenant. Returned entries are masked and operationally useful, but they are not a secret-retrieval mechanism.

<div className="api-meta not-prose"><div className="api-meta-group"><span className="api-meta-label">Method</span><span className="api-badge api-method-get">GET</span></div><div className="api-meta-group"><span className="api-meta-label">Path</span><span className="api-meta-value">/v1/vault/keys</span></div><div className="api-meta-group"><span className="api-meta-label">Base URL</span><span className="api-meta-value">https://overture.igrisinertial.com</span></div><div className="api-meta-group"><span className="api-meta-label">Auth</span><span className="api-meta-value">Session cookie or igris_ API key</span></div><div className="api-meta-group"><span className="api-meta-label">Surface</span><span className="api-badge api-surface-cloud-api">Cloud API</span></div><div className="api-meta-group"><span className="api-meta-label">Stability</span><span className="api-badge api-stability-stable">stable</span></div><div className="api-meta-group"><span className="api-meta-label">Support</span><span className="api-badge api-support-core">core</span></div><div className="api-meta-group"><span className="api-meta-label">Deployment</span><span className="api-badge api-deployment-cloud">cloud</span></div></div>

## When To Use

Use this endpoint when you need to confirm which provider credentials exist, which key names are active, or whether a tenant is ready for hosted routing.

## Integration Guidance

Safe to retry on transient 5xx or 429 responses. If the list is unexpectedly empty, verify tenant context and recent key-management activity before treating it as a server fault.

### Common Mistakes

- Expecting this route to reveal the full upstream key rather than masked metadata.
- Treating an empty list as proof that the vault is broken without first checking the tenant context or provider filter.

### Related Endpoints

- [POST /v1/vault/keys](/docs/api-reference/vault-policy-and-governance/post-v1-vault-keys)
- [GET /v1/vault/keys/:provider](/docs/api-reference/vault-policy-and-governance/get-v1-vault-keys-provider)
- [DELETE /v1/vault/keys/:provider](/docs/api-reference/vault-policy-and-governance/delete-v1-vault-keys-provider)

### Related Guides

- [API Authentication](/docs/api-reference/authentication)
- [API Errors](/docs/api-reference/errors)
- [Key Management](/docs/key-management)

## Query Parameters

| Field | Type | Required | Description |
|---|---|---|---|
| `provider` | `string` | No | Optional provider filter. |

## Code Examples

### cURL

```bash
curl -X GET https://overture.igrisinertial.com/v1/vault/keys?provider=openai \
  -H "Authorization: Bearer $IGRIS_API_KEY"
```

### JavaScript

```javascript
const response = await fetch('https://overture.igrisinertial.com/v1/vault/keys?provider=openai', {
  method: 'GET',
  headers: {
    'Authorization': `Bearer ${process.env.IGRIS_API_KEY}`,
  },
});

const data = await response.text();
console.log(data);
```

### Go

```go
client := &http.Client{}
body := http.NoBody

req, err := http.NewRequest("GET", "https://overture.igrisinertial.com/v1/vault/keys?provider=openai", body)
if err != nil {
	log.Fatal(err)
}

req.Header.Set("Authorization", "Bearer " + os.Getenv("IGRIS_API_KEY"))

resp, err := client.Do(req)
if err != nil {
	log.Fatal(err)
}
defer resp.Body.Close()

payload, _ := io.ReadAll(resp.Body)
fmt.Println(string(payload))
```

### Rust

```rust
let client = reqwest::Client::new();
let response = client
        .request(reqwest::Method::GET, "https://overture.igrisinertial.com/v1/vault/keys?provider=openai")
        .header("Authorization", format!("Bearer {}", std::env::var("IGRIS_API_KEY")?))
        .send()
        .await?;

let payload = response.text().await?;
println!("{}", payload);
```

## Sample Response

```json
{
  "keys": [
    {
      "id": "vault_01HV8Z7R5J8Q4KSY3",
      "provider": "openai",
      "key_name": "production",
      "masked_key": "sk-li...cted",
      "is_active": true,
      "usage_count": 182,
      "created_at": "2026-04-04T06:12:00Z"
    }
  ],
  "count": 1
}
```

## Status Codes

Status-code details are still being documented for this endpoint.

