---
title: "GET /v1/models"
description: "Returns the model identifiers currently visible to the inference layer. Use it to confirm what a deployment can actually serve before you hardcode model selection into a client."
---

# GET /v1/models

Returns the model identifiers currently visible to the inference layer. Use it to confirm what a deployment can actually serve before you hardcode model selection into a client.

<div className="api-meta not-prose"><div className="api-meta-group"><span className="api-meta-label">Method</span><span className="api-badge api-method-get">GET</span></div><div className="api-meta-group"><span className="api-meta-label">Path</span><span className="api-meta-value">/v1/models</span></div><div className="api-meta-group"><span className="api-meta-label">Base URL</span><span className="api-meta-value">https://overture.igrisinertial.com</span></div><div className="api-meta-group"><span className="api-meta-label">Auth</span><span className="api-meta-value">Public</span></div><div className="api-meta-group"><span className="api-meta-label">Surface</span><span className="api-badge api-surface-cloud-api">Cloud API</span></div><div className="api-meta-group"><span className="api-meta-label">Stability</span><span className="api-badge api-stability-stable">stable</span></div><div className="api-meta-group"><span className="api-meta-label">Support</span><span className="api-badge api-support-core">core</span></div><div className="api-meta-group"><span className="api-meta-label">Deployment</span><span className="api-badge api-deployment-cloud">cloud</span></div></div>

## When To Use

Use this endpoint when you need a model picker, startup validation, or an operational check that confirms what the current deployment exposes.

## Integration Guidance

Safe to retry on transient 5xx or 429 responses. If the list looks unexpectedly empty or incomplete, investigate provider configuration before treating it as a temporary server failure.

### Common Mistakes

- Assuming every upstream provider model is available without checking what the deployment currently exposes.
- Using this route as a substitute for tenant provider configuration or entitlement checks.

### Related Endpoints

- [POST /v1/infer](/docs/api-reference/inference-integration/post-v1-infer)
- [POST /v1/chat/completions](/docs/api-reference/inference-integration/post-v1-chat-completions)
- [GET /v1/health](/docs/api-reference/inference-integration/get-v1-health)

### Related Guides

- [API Authentication](/docs/api-reference/authentication)
- [API Errors](/docs/api-reference/errors)
- [First Cloud Integration](/docs/first-cloud-integration)

## Code Examples

### cURL

```bash
curl -X GET https://overture.igrisinertial.com/v1/models
```

### JavaScript

```javascript
const response = await fetch('https://overture.igrisinertial.com/v1/models', {
  method: 'GET',
  headers: {

  },
});

const data = await response.text();
console.log(data);
```

### Go

```go
client := &http.Client{}
body := http.NoBody

req, err := http.NewRequest("GET", "https://overture.igrisinertial.com/v1/models", body)
if err != nil {
	log.Fatal(err)
}


resp, err := client.Do(req)
if err != nil {
	log.Fatal(err)
}
defer resp.Body.Close()

payload, _ := io.ReadAll(resp.Body)
fmt.Println(string(payload))
```

### Rust

```rust
let client = reqwest::Client::new();
let response = client
        .request(reqwest::Method::GET, "https://overture.igrisinertial.com/v1/models")

        .send()
        .await?;

let payload = response.text().await?;
println!("{}", payload);
```

## Sample Response

```json
{
  "object": "list",
  "data": [
    {
      "id": "gpt-4o-mini",
      "object": "model",
      "owned_by": "openai"
    },
    {
      "id": "claude-3-5-sonnet",
      "object": "model",
      "owned_by": "anthropic"
    }
  ]
}
```

## Status Codes

Status-code details are still being documented for this endpoint.

