---
title: "PATCH /v1/actions/:id"
description: "Updates a registered action definition. Fields you omit keep their current values. The same validation as creation applies, including the policy preset and target type vocabulary."
audience: "public"
---

# PATCH /v1/actions/:id

Updates a registered action definition. Fields you omit keep their current values. The same validation as creation applies, including the policy preset and target type vocabulary.

<div className="api-meta not-prose"><div className="api-meta-group"><span className="api-meta-label">Method</span><span className="api-badge api-method-patch">PATCH</span></div><div className="api-meta-group"><span className="api-meta-label">Path</span><span className="api-meta-value">/v1/actions/:id</span></div><div className="api-meta-group"><span className="api-meta-label">Base URL</span><span className="api-meta-value">https://overture.igrisinertial.com</span></div><div className="api-meta-group"><span className="api-meta-label">Auth</span><span className="api-meta-value">Session cookie or igris_ API key</span></div><div className="api-meta-group"><span className="api-meta-label">Surface</span><span className="api-badge api-surface-cloud-api">Cloud API</span></div><div className="api-meta-group"><span className="api-meta-label">Stability</span><span className="api-badge api-stability-stable">stable</span></div><div className="api-meta-group"><span className="api-meta-label">Support</span><span className="api-badge api-support-supported">supported</span></div><div className="api-meta-group"><span className="api-meta-label">Deployment</span><span className="api-badge api-deployment-cloud">cloud</span></div></div>

## When To Use

Use this endpoint when the operation belongs to the Actions surface on the hosted API. It is intended to be the customer-facing contract for this capability.

## Integration Guidance

Do not blindly retry write operations. Retry only on transient 5xx or 429 responses, and make sure the client is idempotent or otherwise safe to replay.

### Common Mistakes

- Using the wrong credential type or forgetting to send the expected API key header.
- Passing a placeholder path segment without replacing it with a real resource identifier.
- Replaying a write request without thinking through idempotency, duplication, or partial success.

### Related Endpoints

- [GET /v1/actions](/docs/api-reference/actions/get-v1-actions)
- [POST /v1/actions](/docs/api-reference/actions/post-v1-actions)
- [GET /v1/actions/:id](/docs/api-reference/actions/get-v1-actions-id)

### Related Guides

- [API Authentication](/docs/api-reference/authentication)
- [API Errors](/docs/api-reference/errors)
- [MCP](/docs/mcp)

## Path Parameters

| Field | Type | Required | Description |
|---|---|---|---|
| `id` | `string` | Yes | Path identifier for `id`. |

## Request Body

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string` | No | New machine name for the action. Omit to keep the current name. |
| `display_name` | `string` | No | Human-readable name shown in the console. Defaults to `name`. |
| `description` | `string` | No | What this action does, for operators and agents. |
| `target_type` | `string` | No | Where the action runs: `hosted_api`, `webhook`, `local_runtime`, `hybrid_fallback`, or `mock_demo`. Defaults to `mock_demo`. |
| `target_url` | `string` | No | Target URL for `hosted_api` and `webhook` actions. Required before those actions can run. |
| `method` | `string` | No | HTTP method used against the target: GET, POST, PUT, PATCH, or DELETE. Defaults to POST. |
| `policy_preset` | `string` | No | Policy preset governing the action: `Safe automation`, `Human-gated`, `Non-replayable`, or `Read-only`. Defaults to `Safe automation`. |
| `replay_class` | `string` | No | Whether a run can be safely replayed during recovery, for example `retryable`. |
| `approval_required` | `boolean` | No | When true, every run pauses for human approval before executing. |
| `irreversible` | `boolean` | No | Marks the side effect as irreversible. Recovery will not automatically replay irreversible work. |
| `secret_refs` | `string[]` | No | Names of secrets the action target needs. References only — secret values are never sent or returned through this API. |
| `target_metadata` | `object` | No | Optional target settings. For `local_runtime` actions, `runtime_id` pins runs to one specific runtime. |

## Request Example

```json
{
  "target_url": "https://billing.example.com/hooks/igris-v2"
}
```

## Code Examples

### cURL

```bash
curl -X PATCH https://overture.igrisinertial.com/v1/actions/example-id \
  -H "Authorization: Bearer $IGRIS_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "target_url": "https://billing.example.com/hooks/igris-v2"
}'
```

### JavaScript

```javascript
const response = await fetch('https://overture.igrisinertial.com/v1/actions/example-id', {
  method: 'PATCH',
  headers: {
    'Authorization': `Bearer ${process.env.IGRIS_API_KEY}`,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
  "target_url": "https://billing.example.com/hooks/igris-v2"
}),
});

const data = await response.text();
console.log(data);
```

### Go

```go
client := &http.Client{}
body := strings.NewReader("{\n  \"target_url\": \"https://billing.example.com/hooks/igris-v2\"\n}")

req, err := http.NewRequest("PATCH", "https://overture.igrisinertial.com/v1/actions/example-id", body)
if err != nil {
	log.Fatal(err)
}
req.Header.Set("Content-Type", "application/json")
req.Header.Set("Authorization", "Bearer " + os.Getenv("IGRIS_API_KEY"))

resp, err := client.Do(req)
if err != nil {
	log.Fatal(err)
}
defer resp.Body.Close()

payload, _ := io.ReadAll(resp.Body)
fmt.Println(string(payload))
```

### Rust

```rust
let client = reqwest::Client::new();
let response = client
        .request(reqwest::Method::PATCH, "https://overture.igrisinertial.com/v1/actions/example-id")
        .header("Authorization", format!("Bearer {}", std::env::var("IGRIS_API_KEY")?))
        .json(&serde_json::json!({
  "target_url": "https://billing.example.com/hooks/igris-v2"
}))
        .send()
        .await?;

let payload = response.text().await?;
println!("{}", payload);
```

## Sample Response

```json
{
  "id": "a3a4e6cb-6c7e-4a5d-9b8f-1d2e3f4a5b6c",
  "name": "send_invoice",
  "display_name": "Send invoice",
  "description": "Send an invoice to a customer through the billing webhook.",
  "target_type": "webhook",
  "target_url": "https://billing.example.com/hooks/igris",
  "method": "POST",
  "policy_preset": "Safe automation",
  "replay_class": "retryable",
  "approval_required": false,
  "irreversible": false,
  "secret_refs": [],
  "fallback_policy": {
    "enabled": false,
    "requires_replay_safe": false
  },
  "created_at": "2026-06-01T08:00:00Z",
  "updated_at": "2026-06-01T08:00:00Z"
}
```

## Status Codes

| Code | Title | Description |
|---|---|---|
| 200 | Success | The request completed successfully. For streaming or download endpoints, the success response may be a stream or attachment rather than a JSON object. |
| 400 | Bad Request | The route was reached, but the request body, query string, or path parameter did not match the endpoint contract. |
| 401 | Unauthorized | Credentials were missing, expired, malformed, or not accepted by this route. |
| 429 | Too Many Requests | The caller exceeded the current throttle window and should wait before retrying. |
| 500 | Server Error | The server accepted the request contract but failed while processing it. |

