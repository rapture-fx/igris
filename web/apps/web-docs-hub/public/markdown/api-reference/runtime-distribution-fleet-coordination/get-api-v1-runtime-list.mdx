---
title: "GET /api/v1/runtime/list"
description: "Lists the runtimes currently registered for a tenant. This is the fleet inventory view customers use to confirm that runtimes are visible, healthy enough to appear in management flows, and correctly associated with the expected tenant."
---

# GET /api/v1/runtime/list

Lists the runtimes currently registered for a tenant. This is the fleet inventory view customers use to confirm that runtimes are visible, healthy enough to appear in management flows, and correctly associated with the expected tenant.

<div className="api-meta not-prose"><div className="api-meta-group"><span className="api-meta-label">Method</span><span className="api-badge api-method-get">GET</span></div><div className="api-meta-group"><span className="api-meta-label">Path</span><span className="api-meta-value">/api/v1/runtime/list</span></div><div className="api-meta-group"><span className="api-meta-label">Base URL</span><span className="api-meta-value">https://overture.igrisinertial.com</span></div><div className="api-meta-group"><span className="api-meta-label">Auth</span><span className="api-meta-value">tenant_id query or authenticated tenant context</span></div><div className="api-meta-group"><span className="api-meta-label">Surface</span><span className="api-badge api-surface-fleet-management">Fleet management</span></div><div className="api-meta-group"><span className="api-meta-label">Stability</span><span className="api-badge api-stability-stable">stable</span></div><div className="api-meta-group"><span className="api-meta-label">Support</span><span className="api-badge api-support-supported">supported</span></div><div className="api-meta-group"><span className="api-meta-label">Deployment</span><span className="api-badge api-deployment-hybrid">hybrid</span></div></div>

## When To Use

Use this endpoint when you need an inventory view before a rollout, during fleet troubleshooting, or when validating that runtime registration is working as expected.

## Integration Guidance

Safe to retry on transient 5xx or 429 responses. If the result is empty, verify tenant context and registration health before assuming a server-side problem.

### Common Mistakes

- Calling the route without tenant context and forgetting to supply the `tenant_id` query parameter.
- Treating this inventory view as if it were a command surface rather than a read model.
- Assuming an absent runtime means rollout should continue before registration health has been checked.

### Related Endpoints

- [GET /v1/runtime/install](/docs/api-reference/runtime-distribution-fleet-coordination/get-v1-runtime-install)
- [GET /v1/runtime/checksum](/docs/api-reference/runtime-distribution-fleet-coordination/get-v1-runtime-checksum)
- [GET /v1/runtime/download](/docs/api-reference/runtime-distribution-fleet-coordination/get-v1-runtime-download)

### Related Guides

- [API Authentication](/docs/api-reference/authentication)
- [API Errors](/docs/api-reference/errors)
- [Hybrid Deployment Workflow](/docs/hybrid-deployment-workflow)

## Query Parameters

| Field | Type | Required | Description |
|---|---|---|---|
| `tenant_id` | `string` | No | Optional explicit tenant identifier when no authenticated tenant context is present. |

## Notes

- When you call this route outside an authenticated console session, include the `tenant_id` query parameter explicitly.

## Code Examples

### cURL

```bash
curl -X GET https://overture.igrisinertial.com/api/v1/runtime/list?tenant_id=tenant_01HV95QXYD8FAPJ
```

### JavaScript

```javascript
const response = await fetch('https://overture.igrisinertial.com/api/v1/runtime/list?tenant_id=tenant_01HV95QXYD8FAPJ', {
  method: 'GET',
  headers: {

  },
});

const data = await response.text();
console.log(data);
```

### Go

```go
client := &http.Client{}
body := http.NoBody

req, err := http.NewRequest("GET", "https://overture.igrisinertial.com/api/v1/runtime/list?tenant_id=tenant_01HV95QXYD8FAPJ", body)
if err != nil {
	log.Fatal(err)
}


resp, err := client.Do(req)
if err != nil {
	log.Fatal(err)
}
defer resp.Body.Close()

payload, _ := io.ReadAll(resp.Body)
fmt.Println(string(payload))
```

### Rust

```rust
let client = reqwest::Client::new();
let response = client
        .request(reqwest::Method::GET, "https://overture.igrisinertial.com/api/v1/runtime/list?tenant_id=tenant_01HV95QXYD8FAPJ")

        .send()
        .await?;

let payload = response.text().await?;
println!("{}", payload);
```

## Sample Response

```json
{
  "runtimes": [
    {
      "runtime_id": "rt_01HV94AS6G98PZ2YH",
      "tenant_id": "tenant_01HV95QXYD8FAPJ",
      "registered_at": "2026-04-04T06:21:00Z",
      "last_heartbeat": "2026-04-04T06:33:00Z",
      "stats": {
        "hostname": "edge-node-01",
        "platform": "linux-amd64"
      }
    }
  ],
  "count": 1
}
```

## Status Codes

| Code | Title | Description |
|---|---|---|
| 200 | Success | Runtime inventory was returned for the resolved tenant. |
| 400 | Missing tenant | The request did not include an authenticated tenant context or a `tenant_id` query parameter. |

### 400 Example

```json
{
  "error": "missing_tenant_id",
  "message": "tenant_id query parameter or authentication required"
}
```

