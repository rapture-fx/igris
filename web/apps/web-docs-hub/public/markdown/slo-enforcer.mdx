---
title: "SLO Enforcer"
description: "How Igris evaluates runtime and routing health against latency, error-rate, availability, and throughput objectives, and what is customer-facing today."
---

# SLO Enforcer

Igris uses SLO enforcement to keep routing and runtime execution inside operational bounds. The enforcer evaluates live metrics, classifies breaches, and produces remediation actions that the platform can execute or record.

The current customer-facing contract is not a standalone SLO configuration API. Customers see SLO outcomes through routing behavior, provider health, execution receipts, history, and observability views.

---

## What Is Enforced

The production enforcer evaluates these metric inputs:

| Metric | Target | Warning | Critical |
|---|---:|---:|---:|
| `p99_latency_ms` | `100` | `130` | `150` |
| `p95_latency_ms` | `50` | `80` | `100` |
| `error_rate` | `0.001` | `0.01` | `0.02` |
| `availability` | `0.9999` | `0.999` | `0.99` |
| `throughput_rps` | `10000` | `8000` | `5000` |

The enforcer is configured with a 20-second evaluation interval and a 300-second breach cooldown. Auto-remediation is enabled in the default production configuration.

---

## Evaluation Output

The Rust SLO enforcer exposes an internal FFI contract that accepts a metrics JSON payload and returns a breach decision plus remediation actions:

```json
{
  "breached": true,
  "actions": [
    {
      "action_type": "reroute",
      "target": "provider_pool",
      "reason": "P99 latency exceeded critical threshold",
      "slo_type": "P99Latency",
      "current_value": 200,
      "threshold_value": 150
    }
  ],
  "timestamp": 1712213400
}
```

This is an internal runtime/control-plane contract, not a customer API route.

---

## Customer-Facing Surfaces

Use these documented surfaces to understand SLO behavior:

- [Provider Health](/docs/provider-health) for provider status and degradation signals.
- [Circuit Breaker](/docs/circuit-breaker) for provider trip state and recovery behavior.
- [Routing Control & Analytics](/docs/api-reference/routing-control-analytics/get-v1-routing-stats) for routing telemetry.
- [Execution Receipts](/docs/execution-receipts) for receipt-level evidence that an execution completed, failed, or violated policy.
- [Observability & Tracing](/docs/observability-tracing) for deployment-level monitoring.

---

## Operational Admin Handlers

Some deployments wire internal admin handlers for SLO operations:

| Method | Path | Purpose |
|---|---|---|
| `GET` | `/admin/slo/status` | Inspect enforcer status and recent events |
| `GET` | `/admin/slo/audit` | Read recent SLO audit events |
| `POST` | `/admin/slo/evaluate` | Run a manual metrics evaluation |
| `GET` | `/admin/slo/metrics` | Export Prometheus-style SLO counters |

These handlers are operational/admin surfaces. They are intentionally not listed in the customer API reference until they are promoted to a supported customer contract.

---

## Integration Guidance

Do not build customer automation against standalone SLO configuration or compliance routes; they are not part of the current production API.

For customer integrations:

- Use routing and provider-health APIs to observe whether the platform is routing around degraded providers.
- Use receipts and history for audit evidence.
- Use deployment monitoring for SLO dashboards.
- Treat remediation as platform behavior unless your deployment has explicitly exposed an admin SLO surface.

---

[Provider Health](/docs/provider-health) | [Circuit Breaker](/docs/circuit-breaker) | [Observability & Tracing](/docs/observability-tracing)
