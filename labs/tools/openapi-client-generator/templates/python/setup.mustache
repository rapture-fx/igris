# coding: utf-8

"""
    {{appName}}

    {{appDescription}}{{^appDescription}}{{packageName}}{{/appDescription}}  # noqa: E501

    The version of the OpenAPI document: {{appVersion}}{{#version}}{{.}}{{/version}}
    {{#contact}}{{#contactName}}Contact: {{contactName}}{{/contactName}}{{#contactUrl}} ({{contactUrl}}){{/contactUrl}}{{#contactEmail}} <{{contactEmail}}>{{/contactEmail}}{{/contact}}
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""

from setuptools import setup, find_packages

NAME = "{{packageName}}"
VERSION = "{{packageVersion}}"
# To install the library, run the following
#
# python setup.py install
#
# prerequisite: setuptools
# http://pypi.python.org/pypi/setuptools

REQUIRES = [
    "urllib3 >= 1.25.3, < 2.1.0",
    "python-dateutil",
    "pydantic >= 1.10.5, < 3",
    "typing-extensions >= 4.3.0",
{{#asyncio}}
    "aiohttp >= 3.8.0",
    "aiofiles >= 0.12.0",
{{/asyncio}}
]

{{#packageUrl}}
PACKAGE_URL = "{{packageUrl}}"
{{/packageUrl}}
{{^packageUrl}}
PACKAGE_URL = ""
{{/packageUrl}}

# Additional metadata for enhanced PyPI listing
KEYWORDS = [
    "data-processing",
    "machine-learning", 
    "api-client",
    "igris-inertial",
    "openapi",
    "rest-api"
]

CLASSIFIERS = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Intended Audience :: Information Technology",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.7",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Database",
    "Typing :: Typed"
]

# Read long description from README
try:
    with open("README.md", "r", encoding="utf-8") as fh:
        long_description = fh.read()
except FileNotFoundError:
    long_description = "{{appDescription}}{{^appDescription}}Python client library for {{appName}}{{/appDescription}}"

# Read version from __init__.py if available
try:
    import os
    import re
    here = os.path.abspath(os.path.dirname(__file__))
    with open(os.path.join(here, "{{packageName}}", "__init__.py"), "r") as f:
        version_file = f.read()
    version_match = re.search(r"^__version__ = ['\"]([^'\"]*)['\"]", version_file, re.M)
    if version_match:
        VERSION = version_match.group(1)
except (FileNotFoundError, ImportError):
    pass

setup(
    name=NAME,
    version=VERSION,
    description="{{appDescription}}{{^appDescription}}{{packageName}}{{/appDescription}}",
    long_description=long_description,
    long_description_content_type="text/markdown",
    author="{{packageCompany}}{{^packageCompany}}OpenAPI Generator community{{/packageCompany}}",
    author_email="{{packageEmail}}{{^packageEmail}}support@igris-inertial.com{{/packageEmail}}",
    url=PACKAGE_URL,
    project_urls={
        "Bug Reports": "{{packageUrl}}/issues",
        "Source": "{{packageUrl}}",
        "Documentation": "https://docs.igris-inertial.com",
        "Changelog": "{{packageUrl}}/releases",
    },
    keywords=KEYWORDS,
    python_requires=">=3.7",
    install_requires=REQUIRES,
    packages=find_packages(exclude=["test", "tests", "test_*", "*.test", "*.test.*"]),
    include_package_data=True,
    license="{{licenseName}}{{^licenseName}}MIT{{/licenseName}}",
    classifiers=CLASSIFIERS,
    extras_require={
        "dev": [
            "pytest>=6.2.0",
            "pytest-cov>=2.12.0",
            "flake8>=3.9.0",
            "black>=21.5b0",
            "isort>=5.9.0",
            "mypy>=0.910",
            "bandit>=1.7.0",
            "safety>=1.10.0",
        ],
        "async": [
            "aiohttp>=3.8.0",
            "aiofiles>=0.12.0",
        ],
        "testing": [
            "pytest>=6.2.0",
            "pytest-asyncio>=0.15.0",
            "pytest-mock>=3.6.0",
            "responses>=0.13.0",
            "requests-mock>=1.9.0",
        ]
    },
    zip_safe=False,
    # Entry points for command-line tools if needed
    entry_points={
        'console_scripts': [
            # Add CLI tools here if needed
        ],
    },
    # Support for type hints
    package_data={
        "{{packageName}}": ["py.typed"],
    },
)